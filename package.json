{
  "name": "bailey-forbes-portfolio",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "sync:metadata": "node scripts/sync-project-metadata.mjs",
    "sync:metadata:check": "node scripts/sync-project-metadata.mjs --check",
    "pretest": "npm run build",
    "test": "node --test \"tests/**/*.test.mjs\""
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260916.1",
    "@fortawesome/fontawesome-free": "^6.7.2",
    "typescript": "5.9.2",
    "wrangler": "^4.132.0"
  }
}
